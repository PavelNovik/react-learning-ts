import type { StoryObj} from '@storybook/react';
import Select from "./Select";

export default {
    title: "React/Select/Select",
    component: Select
}

type Story = StoryObj<typeof Select>;

export const FirstSelectStory: Story = {
    args: {


    },
}